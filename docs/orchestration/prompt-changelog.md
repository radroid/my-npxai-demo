# Prompt changelog

The SINGLE version record for the role templates (`docs/orchestration/prompts/*.md`) — templates carry no version headers. Every template change lands here tied to the KPI it targets. The steward also logs "no change" audit traces here every run.

## KPI definitions

- **planner/executor/reviewer cost** — approx tokens per run (from token-ledger).
- **review yield** — real issues per review that SURVIVE the fix (issue applied, not reverted/rejected).
- **escaped defects** — defects found after merge. Until a runtime smoke oracle exists, label this "escaped defects (build/unit-detectable only)" — it is 0-known, not 0-real.
- **plan-friction count** — friction-log entries attributable to spec gaps.
- **cycle overhead** — fix loops per PR.

## Changes

- **2026-07-14 — Steward pass (item-1 gate, evidence through PR #8).** Four template edits, each mapped 1:1 to a friction-log entry that already cost a run:
  - `planner.md` — added contract items 5–8. Item 5 (two bullets): cross-check that an interaction affordance is never bound to a control another requirement disables during the same window (source: `docs/orchestration/specs/item-1-artifact-mode.md:79` bound Escape-to-abort to an input the same spec required disabled during a run — the executor had to silently invent the real fix, PR #7 Execution notes line 203, with zero spec guidance); and reconcile element/attribute allowlists (source: PR #6's SVG `<marker>` spec omitted `id`/`marker-end` while listing `marker`/`defs`). Item 6: verify corpus/data facts against the live source of record, not README/PLAN prose (source: item-2 spec said "15 REGDOCs," live corpus is 19 docs/~1,945 chunks). Item 7: external-service liveness edge case (source: Supabase found paused mid-slice on item-2, third recurrence). Item 8: sibling-item collision check (source: item-1 and item-2 planners independently spec'd the same `lib/retrieval.ts` extraction, caught only at orchestrator level). Targets: **plan-friction count**.
  - `executor.md` — added contract item 2 (liveness preflight before starting corpus-dependent implementation) and item 5 (checkpoint-push + DELTA/invariant-tagged comments for any slice touching more than ~4 files). This is the highest-value rule in this pass: three long executor runs were killed mid-slice by infra (usage limits, stream stalls) across item-1/item-2, and recovery was cheap in exactly the ONE case that had already checkpoint-pushed with `// ADDITIVE (item-2 DELTA D1)`-style tagging — the other deaths left code with intent that had to be reconstructed by hand. Targets: **cycle overhead** (recovery cost after infra death) and **plan-friction count** (wasted setup on a dead dependency).
  - `reviewer.md` — contract item 1 extended so "never fall back to the local tree" also bans citing local-tree state as evidence for ANY claim, not only the diff-fetch-failure case (source: PR #5 reviewer asserted stale local-tree state as fact). Added ANTI-BIAS rule 7: the blind hostile smell pass is now MANDATORY on every review, not conditional on a long zero-block streak, plus a `## Smell pass` report field. Source: this exact pass was the only thing that caught PR #6's unreachable limited-coverage callout — a real escaped defect that six approving claims missed. Targets: **escaped defects** — trading a small fixed per-review cost increase for closing the highest-value miss observed so far. Per QUALITY OUTRANKS TOKEN SAVINGS, this is a deliberate cost increase, not a saving, and is not subject to reverting on token grounds.
  - `fix.md`, `steward.md` — reviewed through PR #8, **no change**. No friction-log entry or ledger outlier this pass implicated either role's contract.
  - Three friction entries reviewed and deliberately left with **no template change** (kept in friction-log, one moved to `## Resolved` as already handled, two left `## Open`): (1) the `gh pr merge` permission-gate quirk (PR #5) governs the ORCHESTRATOR's own merge sequencing — no orchestrator template exists among the five to encode it, and the workaround (reviewer pass before every merge) is already adopted in practice; (2) the `withGuard`/`logRequest` post-log field timing bug (PR #6) is a real product defect, not a process gap — stays Open as a backlog candidate, not a template fix; (3) the Workflow `args` JSON-string-encoding bug that caused a 300k-token misfire review on PR #6 lives in `assets/review-and-verify.workflow.js`, which is outside `docs/orchestration/` and outside the steward's edit scope this run — moved to Resolved since the counter-move (inline claims via scriptPath) is already adopted at the dispatch layer, but flagged as needing an upstream canon-script fix (fail loudly when args are absent) from whoever owns that file.
- **2026-07-14 — Model tiering revert condition (audit trace, no template edit).** Firming up the revert condition on the tiering entry directly below: reviewer cost ran ~2.446M tokens untiered (PR #6, 31 agents, 30/30 landed) vs ~1.894M tiered (PR #7 re-review) — and the tiered run landed 30/30 ballots where the two untiered attempts on the SAME PR #7 review died at 19/30 and 1/30 to usage-limit exhaustion before tiering existed, so tiering is currently winning on both cost AND completion rate, not cost alone. Concrete revert triggers (supersedes the prior open-ended "if yield drops" phrasing): revert if (a) a mid/low-tier refuter ballot is shown to have missed a defect that a full-tier rerun on the same claim catches, (b) review yield (real issues surviving the fix) drops below the untiered PR #6 baseline for 2 consecutive reviewed PRs, or (c) ballot completion rate falls below 100% for a TIER-specific reason (as opposed to session-wide usage-limit exhaustion, which hits either configuration equally). Current signal: 0/3 triggers tripped — tiering stays. Targets: **reviewer cost**, guarded against **escaped defects**.
- **2026-07-14** — **Steward review round 1** (targets: cycle overhead + escaped defects). Reviewer of the steward's own PR #9 caught three things, all fixed: (1) executor rules 4/5 contradicted each other — "full gate before push" vs "checkpoint-push mid-slice" left an executor stuck between shipping non-green code and surviving infra death; the gate now explicitly scopes to the FINAL push and exempts `wip(...)` checkpoints. (2) The mandatory smell pass lost canon's "independent lens / different model" framing and degenerated into self-re-reading; restored — a fresh hostile agent with no memory of why you approved is the target, self-review is the fallback. (3) Friction bookkeeping filed one no-template-change entry under Resolved while two identical siblings sat under Open; unified on one criterion. Free-hunt also caught that the item-2 human blockers lived only in `manual-verification.md` while CLAUDE.md makes `TODO.md` the canonical human queue — mirrored into TODO.md Phase 11.
- **2026-07-14** — **Model tiering in the review gate** (targets: reviewer cost, without touching review yield). The canon review workflow previously let all 30 refuters inherit the session model — millions of tokens and a tripped usage limit that fail-closed a whole verdict into noise. Refuter lenses are now tiered per `agent()` call: `free-hunt` + `composition` (the two that need real reasoning — invent an unlisted failure mode / re-read cross-PR contracts) stay on the top tier at high effort, and the blind-hostile smell probe stays top-tier (it is what caught PR #6's escaped defect); `hostile`, `delta-consequence`, `no-self-marked-homework` drop to a mid-tier model at low/medium effort since they are mechanical verification against a stated contract. QUALITY OUTRANKS TOKEN SAVINGS still binds: if review yield drops or a defect escapes, revert the tiering — the changelog decides whether it stays.
- **2026-07-13** — Seed. All five templates authored from the orchestrated-delivery loop contract + ANTI-BIAS clauses (reviewer embeds them verbatim; unified verdict grammar `APPROVE | REVISE — n | BLOCK — n`). Caveman report style encoded in every template's report section. No KPI data yet.
