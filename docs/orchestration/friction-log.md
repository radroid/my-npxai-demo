# Friction log

Anything that slowed a role down or forced a workaround. Entry format: `- [role-tag] (date, run) problem — implication`. The steward drains `## Open` into template changes and moves entries to `## Resolved` with a pointer to the change.

## Open

- [orchestrator] (2026-07-13, PR 5) permission gate denies `gh pr merge` without prior review evidence — loop adapted: every PR (even docs-only) gets a reviewer pass before the merge attempt; merge command description must cite the review verdict. No template change — this governs the ORCHESTRATOR's own merge sequencing, not a dispatched role, and no orchestrator template exists among the five to encode it. Already self-corrected in practice (every PR now gets a reviewer pass pre-merge). Left Open as a standing note for whoever authors an orchestrator-facing doc later, rather than filed as Resolved-by-template-change.
- [executor] (2026-07-13, PR 6) `withGuard` fires `logRequest` at handler return but SSE work continues after — `output_tokens`/`artifact_bytes` land in ctx.logFields post-log on ALL streaming routes (pre-existing on chat/generator too). No template change — this is a real product bug, not a process/template gap; stays Open as a backlog candidate for a later hardening item, not this sprint's steward pass.
- [orchestrator] (2026-07-13, PR 6 review) Workflow `args` arrived inside the script as a JSON-encoded STRING, so the canon review script silently reviewed its inline DEMO claims instead of PR #6 — 300k tokens burned on a meaningless verdict. Counter-move already adopted in practice: inline claims directly in the script body via scriptPath, never rely on `args`. No template change — the bug lives in the canon script (`assets/review-and-verify.workflow.js`), outside `docs/orchestration/` and outside the steward's edit scope. Stays Open (same criterion as the two entries above: adopted-in-practice but NOT closed by a template change) as a standing note for an upstream fix — the canon script should fail loudly when args are absent rather than silently falling back to demo claims.

- [orchestrator] (2026-07-14, PR 13 review) an **in-place** embedding model+dim swap (`vector(1536)` → `halfvec(3072)`, RPC signature changed) COUPLES the Worker code deploy to the DB migration such that a production outage window is *guaranteed* in either merge order — the new RPC signature is incompatible with the old deployed code AND the old signature is incompatible with the new code, so there is no sequencing that avoids a gap; on top of that the migration DROPs the live embeddings until a paid re-ingest lands. The adversarial review (deploy-ordering REVISE, 0.92–0.95 across 3 lenses) confirmed the branch code is correct for the target state — the defect is deploy *sequencing*, not code. Counter-move adopted: HOLD the merge behind a hosted runbook (pause cron → optional embedding snapshot → `db push` → re-ingest via staging swap → merge → un-pause), never merge the 3072 client ahead of the DB. No template change — this is an orchestrator-facing deploy-sequencing rule with no dispatched-role template to encode it (same criterion as the three entries above); the reusable lesson is also saved to agent memory (`feedback_inplace_schema_swap_couples_deploy`). Considered and REJECTED a code fix (fail-open-to-OOS on RPC error) because it would mask genuine DB outages as "empty corpus" — the exact fail-open trap from the PR #11 lesson.

- [orchestrator] (2026-07-14, PR 13 post-deploy) the embedding upgrade improved RANKING (hit@8 95.7→96.7, recall 82.6→86.0, MRR 0.761→0.808) but INTRODUCED a generation-side regression: stronger retrieval surfaced adjacent-doc chunks confidently enough that an out-of-corpus probe naming an absent REGDOC (ooc-017) got a misattributed answer — negative rejection 100→97.3%. The headless `ksweep` (ranking-only; calls retrieveChunks directly, never the OOS gate or generation) structurally could NOT catch it; only the server-backed `negative` eval did. Fixed with prompt rule 2d (attribution grounding), restored 37/37, verified zero golden over-rejection by testing ALL 34 named-doc golden questions (bounded the at-risk population and tested all of it). No template change — this is an eval-coverage/orchestrator lesson saved to memory as `feedback_component_win_can_regress_pipeline`; the standing takeaway is "re-run end-to-end behavior evals after a component swap, not just the component's own metric," which no dispatched-role template owns.

## Resolved

- [reviewer] (2026-07-13, PR 5) reviewer asserted local-working-tree state ("timelapse files still present") that was factually stale/wrong — its own contract says diff + `gh` only. **Fix:** `docs/orchestration/prompts/reviewer.md` contract item 1 now states explicitly that no claim may cite local working-tree state as evidence — only `gh pr diff`/`gh pr view`/`gh api` output is admissible. Targets: escaped defects / review trustworthiness.
- [orchestrator] (2026-07-13/14, PR 7 + item-2) INFRA, not process: the Fable 5 usage limit killed 29/30 refuters on the PR #7 review (fail-closed REVISE was pure dead-ballot noise) and stalled the slice-2.1 executor three times. Counter-moves already adopted at dispatch time: (a) checkpoint-commit-and-push protocol for long executor runs; (b) `resumeFromRunId` replays surviving ballots from cache; (c) re-dispatch under another model tier when one is exhausted. **Fix (this pass):** generalized (a) into a standing rule — `docs/orchestration/prompts/executor.md` contract item 5 now requires checkpoint-push for any slice touching more than ~4 files, so this doesn't depend on an individual executor's judgment call. Targets: cycle overhead / executor cost (recovery cost after infra death).
- [executor] (2026-07-14, PR 8) two prior executor deaths left CODE but no execution notes; intent had to be reconstructed from inline comments. The checkpoint's `// ADDITIVE (item-2 DELTA D1)` tagging convention is the only reason recovery was cheap. **Fix:** folded into the same `executor.md` contract item 5 as a standing rule — tag every checkpoint edit with the DELTA/invariant it serves. Targets: cycle overhead (recovery cost).
- [orchestrator] (2026-07-14, item-2) Supabase paused for the THIRD time, discovered only when an executor was already mid-slice on corpus-dependent work — cost the golden set and blocked all of slice 2.2. **Fix:** `docs/orchestration/prompts/planner.md` contract item 7 requires an external-service liveness edge case for any item whose slice depends on Supabase/a third-party API; `docs/orchestration/prompts/executor.md` contract item 2 requires an executor-side liveness preflight before starting corpus-dependent implementation. Targets: plan-friction count / wasted executor setup time.
- [planner] (2026-07-13, item-1) spec's SVG attribute contract omitted `id` on `<marker>` + `marker-end` while the element list included marker/defs — executor had to deviate to make arrows functional. **Fix:** `docs/orchestration/prompts/planner.md` contract item 5 (second bullet) requires element/attribute allowlist reconciliation — trace how each listed element is wired and confirm its required attributes are present. Targets: plan-friction count.
- [planner] (2026-07-13, item-1) spec (`docs/orchestration/specs/item-1-artifact-mode.md:79`) bound Escape-to-abort to the workbench input while the SAME spec required that input be disabled during a run — the input's keydown can never fire while disabled, so the executor had to silently invent the real fix (a window-level listener gated on visibility/run-state, PR #7 Execution notes line 203) with zero spec guidance and no chance to negotiate it back. Not previously filed as a friction-log entry (resolved inline as an executor "deviation" instead) — backfilled here from the spec's Execution notes and token-ledger evidence since it is a real, recurring-shape defect class (contradictory control-state requirements). **Fix:** `docs/orchestration/prompts/planner.md` contract item 5 (first bullet) requires planners to cross-check that any interaction affordance is compatible with every other requirement on the same control's state, and to say explicitly where the affordance actually lives if the primary target will be disabled. Targets: plan-friction count.
- [planner] (2026-07-13, item-2) docs said "15 REGDOCs" but corpus truth is 19 docs / ~1,945 chunks; `scraped_regdocs/` being gitignored killed the file-based golden-gen option late — backlog now names corpus-of-record = Supabase DB. **Fix:** `docs/orchestration/prompts/planner.md` contract item 6 requires verifying corpus/data facts against the LIVE source of record before citing counts/schema, and flagging any referenced local artifact that won't be available to the executor. Targets: plan-friction count.
- [orchestrator] (2026-07-13, item-1/item-2) both planners independently spec'd the SAME `lib/retrieval.ts` extraction — cross-item dependency only caught at orchestrator level; backlog `Needs` column now records it for these two items. **Fix:** `docs/orchestration/prompts/planner.md` contract item 8 adds a standing sibling-item collision check so future overlapping items get caught by the planner itself, not only by the orchestrator noticing after the fact. Targets: plan-friction count.
- [planner] (2026-07-14, item-2/PR 8) the spec's R7 metric catalog defined each metric's formula but only implicitly its exclusion policy — three adversarial review rounds each rediscovered the SAME vacuous-pass bug class in a new spot the spec had not pinned down (baseline citation validity in round 1; the identical shape in `consistency`/`paraphrase` in round 2; answer relevancy plus the framework's first-ever live-HTTP error handling in round 3), instead of the whole class being caught once at spec time. **Fix:** `docs/orchestration/prompts/planner.md` contract item 9 requires the edge-cases section to enumerate, per aggregate/scored metric AND per experiment/branch that computes it, the vacuous/illegitimate cases (empty response, zero citations, refusal, no-LLM-call branch, malformed/non-2xx response) and the exclusion policy, plus a full-denominator coverage companion. Targets: plan-friction count, cycle overhead.
- [fix] (2026-07-14, PR 8 fix rounds 1→2) round 1 fixed citation-validity's vacuous-pass bug in the `baseline` experiment only; round 2 found the IDENTICAL bug still live in `consistency` (sitting under the headline citation-stability KPI) and in `paraphrase` (three separate vacuous passes at once) — because nothing required sweeping every call site sharing the bug's shape before declaring the issue closed. **Fix:** `docs/orchestration/prompts/fix.md` contract item 6 requires grepping every call site/experiment sharing a reviewer-named bug shape and listing every site checked (fixed + confirmed-clean), not only the cited one; `docs/orchestration/prompts/reviewer.md` ANTI-BIAS 8 requires a fix-round review to explicitly check sibling call sites for the same shape before approving. Targets: cycle overhead, escaped defects.
- [fix] (2026-07-14, PR 8 fix round 2) round 1's own regression test for issue 2 was itself partly vacuous — a `.slice(0, 30)` truncated the assertion so half of what it appeared to check was never actually checked — caught only because round 2 retroactively mutation-tested it (revert the fix, confirm the suite goes RED, restore). **Fix:** `docs/orchestration/prompts/fix.md` contract item 7 makes mutation-testing a standing requirement for every fix round's regression tests, not a voluntary practice; the report must state `n/n mutations caught`. Targets: escaped defects.
- [fix] (2026-07-14, PR 8 fix round 3) fix round 3 misreported its own regression-suite size in its report; the orchestrator caught the discrepancy only by re-running the suite itself (actual: 266 `ok` lines — reproducible via the command table in `docs/orchestration/test-counts.md`, which exists because the PR #10 reviewer rightly refused an asserted count as evidence for a rule ABOUT asserted counts). **Fix:** `docs/orchestration/prompts/fix.md` contract item 8 requires quoting the runner's own printed tally verbatim in the report and commit message, never a remembered or estimated count. Targets: cycle overhead (the wasted orchestrator verification cycle this rule exists to prevent).
