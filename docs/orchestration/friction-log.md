# Friction log

Anything that slowed a role down or forced a workaround. Entry format: `- [role-tag] (date, run) problem — implication`. The steward drains `## Open` into template changes and moves entries to `## Resolved` with a pointer to the change.

## Open

- [orchestrator] (2026-07-13, PR 5) permission gate denies `gh pr merge` without prior review evidence — loop adapted: every PR (even docs-only) gets a reviewer pass before the merge attempt; merge command description must cite the review verdict. No template change — this governs the ORCHESTRATOR's own merge sequencing, not a dispatched role, and no orchestrator template exists among the five to encode it. Already self-corrected in practice (every PR now gets a reviewer pass pre-merge). Left Open as a standing note for whoever authors an orchestrator-facing doc later, rather than filed as Resolved-by-template-change.
- [executor] (2026-07-13, PR 6) `withGuard` fires `logRequest` at handler return but SSE work continues after — `output_tokens`/`artifact_bytes` land in ctx.logFields post-log on ALL streaming routes (pre-existing on chat/generator too). No template change — this is a real product bug, not a process/template gap; stays Open as a backlog candidate for a later hardening item, not this sprint's steward pass.

## Resolved

- [reviewer] (2026-07-13, PR 5) reviewer asserted local-working-tree state ("timelapse files still present") that was factually stale/wrong — its own contract says diff + `gh` only. **Fix:** `docs/orchestration/prompts/reviewer.md` contract item 1 now states explicitly that no claim may cite local working-tree state as evidence — only `gh pr diff`/`gh pr view`/`gh api` output is admissible. Targets: escaped defects / review trustworthiness.
- [orchestrator] (2026-07-13/14, PR 7 + item-2) INFRA, not process: the Fable 5 usage limit killed 29/30 refuters on the PR #7 review (fail-closed REVISE was pure dead-ballot noise) and stalled the slice-2.1 executor three times. Counter-moves already adopted at dispatch time: (a) checkpoint-commit-and-push protocol for long executor runs; (b) `resumeFromRunId` replays surviving ballots from cache; (c) re-dispatch under another model tier when one is exhausted. **Fix (this pass):** generalized (a) into a standing rule — `docs/orchestration/prompts/executor.md` contract item 5 now requires checkpoint-push for any slice touching more than ~4 files, so this doesn't depend on an individual executor's judgment call. Targets: cycle overhead / executor cost (recovery cost after infra death).
- [executor] (2026-07-14, PR 8) two prior executor deaths left CODE but no execution notes; intent had to be reconstructed from inline comments. The checkpoint's `// ADDITIVE (item-2 DELTA D1)` tagging convention is the only reason recovery was cheap. **Fix:** folded into the same `executor.md` contract item 5 as a standing rule — tag every checkpoint edit with the DELTA/invariant it serves. Targets: cycle overhead (recovery cost).
- [orchestrator] (2026-07-14, item-2) Supabase paused for the THIRD time, discovered only when an executor was already mid-slice on corpus-dependent work — cost the golden set and blocked all of slice 2.2. **Fix:** `docs/orchestration/prompts/planner.md` contract item 7 requires an external-service liveness edge case for any item whose slice depends on Supabase/a third-party API; `docs/orchestration/prompts/executor.md` contract item 2 requires an executor-side liveness preflight before starting corpus-dependent implementation. Targets: plan-friction count / wasted executor setup time.
- [planner] (2026-07-13, item-1) spec's SVG attribute contract omitted `id` on `<marker>` + `marker-end` while the element list included marker/defs — executor had to deviate to make arrows functional. **Fix:** `docs/orchestration/prompts/planner.md` contract item 5 (second bullet) requires element/attribute allowlist reconciliation — trace how each listed element is wired and confirm its required attributes are present. Targets: plan-friction count.
- [planner] (2026-07-13, item-1) spec (`docs/orchestration/specs/item-1-artifact-mode.md:79`) bound Escape-to-abort to the workbench input while the SAME spec required that input be disabled during a run — the input's keydown can never fire while disabled, so the executor had to silently invent the real fix (a window-level listener gated on visibility/run-state, PR #7 Execution notes line 203) with zero spec guidance and no chance to negotiate it back. Not previously filed as a friction-log entry (resolved inline as an executor "deviation" instead) — backfilled here from the spec's Execution notes and token-ledger evidence since it is a real, recurring-shape defect class (contradictory control-state requirements). **Fix:** `docs/orchestration/prompts/planner.md` contract item 5 (first bullet) requires planners to cross-check that any interaction affordance is compatible with every other requirement on the same control's state, and to say explicitly where the affordance actually lives if the primary target will be disabled. Targets: plan-friction count.
- [planner] (2026-07-13, item-2) docs said "15 REGDOCs" but corpus truth is 19 docs / ~1,945 chunks; `scraped_regdocs/` being gitignored killed the file-based golden-gen option late — backlog now names corpus-of-record = Supabase DB. **Fix:** `docs/orchestration/prompts/planner.md` contract item 6 requires verifying corpus/data facts against the LIVE source of record before citing counts/schema, and flagging any referenced local artifact that won't be available to the executor. Targets: plan-friction count.
- [orchestrator] (2026-07-13, PR 6 review) Workflow `args` arrived inside the script as a JSON-encoded STRING, so the canon review script silently reviewed its inline DEMO claims instead of PR #6 — 300k tokens burned on a meaningless verdict. Counter-move already adopted: inline claims directly in the script body via scriptPath, never rely on `args`. **No template change** — this is a Workflow-runner dispatch/argument-passing bug in the canon script (`assets/review-and-verify.workflow.js`), which lives outside `docs/orchestration/` and outside the steward's edit scope; not a role-template gap. Worth an upstream fix to the canon script (fail loudly when args are absent) but that is not this pass's job.
- [orchestrator] (2026-07-13, item-1/item-2) both planners independently spec'd the SAME `lib/retrieval.ts` extraction — cross-item dependency only caught at orchestrator level; backlog `Needs` column now records it for these two items. **Fix:** `docs/orchestration/prompts/planner.md` contract item 8 adds a standing sibling-item collision check so future overlapping items get caught by the planner itself, not only by the orchestrator noticing after the fact. Targets: plan-friction count.
